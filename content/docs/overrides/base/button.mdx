---
title: Button
description: Button component using @base-ui/react/button
---

The Button uses `@base-ui/react/button` as the primitive. Install when your style is `base-nova`,
`base-vega`, etc.

## Install

```bash
npx shadcn@latest add @uxio/button
```

Configure your registry in `components.json`:

```json
{
  "registries": {
    "@uxio": "https://ui.uxio.dev/r/styles/{style}/{name}.json"
  }
}
```

The correct variant (base or radix) is resolved automatically from your `components.json` style.

## Extended Features

This override extends the original shadcn Button with additional semantic color variants.

- [Semantic Colors](#semantic-colors)
- [Success Variant](#success-variant)
- [Info Variant](#info-variant)
- [Warning Variant](#warning-variant)
- [Loading](#loading)

### Semantic Colors

Three new CSS variables are added to the theme (with dark mode equivalents). Add these to your
`:root` and `.dark` in your global CSS:

```css
:root {
  --success: oklch(0.62 0.36 145.52);
  --info: oklch(0.71 0.45 250);
  --warning: oklch(0.73 0.16 72.83);
}

.dark {
  --success: oklch(0.68 0.3 145.52);
  --info: oklch(0.72 0.38 250);
  --warning: oklch(0.78 0.14 72.83);
}
```

### Success Variant

For positive actions like confirmations or completed states.

<ComponentPreview name="base/button-success" />

### Info Variant

For informational actions or neutral highlights.

<ComponentPreview name="base/button-info" />

### Warning Variant

For cautionary actions that require attention.

<ComponentPreview name="base/button-warning" />

### Loading

Use the `loading` prop to show a spinner and disable the button during async actions.

<ComponentPreview name="base/button-loading" />

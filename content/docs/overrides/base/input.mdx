---
title: Input
---

## Extended Features

- [Size variants](#size-variants) — `xs`, `sm`, `default`, and `lg` with the same dimensions and spacing as the Button component

## Install

Configure your registry in `components.json`:

```json
{
  "registries": {
    "@uxio": "https://ui.uxio.dev/r/styles/{style}/{name}.json"
  }
}
```

Then run:

```bash
npx shadcn@latest add @uxio/input
```

The correct variant (base or radix) is resolved automatically from your `components.json` style.

## Examples

### Size Variants

Use the `size` prop to match input dimensions with the Button component. Sizes align across all styles (lyra, vega, nova, mira, maia).

### Extra Small (xs)

<ComponentPreview name="base/input-size-xs" />

### Small (sm)

<ComponentPreview name="base/input-size-sm" />

### Default

<ComponentPreview name="base/input-size-default" />

### Large (lg)

<ComponentPreview name="base/input-size-lg" />

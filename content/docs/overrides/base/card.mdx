---
title: Card
---

## Extended Features

- Footer has now a subtlemuted background. [Example](#default-variant)
- Added [Destructive Variant](#destructive-variant)

## Install

Configure your registry in `components.json`:

```json
{
  "registries": {
    "@uxio": "https://ui.uxio.dev/r/styles/{style}/{name}.json"
  }
}
```

Then run:

```bash
npx shadcn@latest add @uxio/card
```

The correct variant (base or radix) is resolved automatically from your `components.json` style.

## Examples

### Default Variant

The default variant provides standard card styling.

<ComponentPreview name="base/card-default" />

### Destructive Variant

Use the destructive variant for warnings, errors, or destructive actions. It adds a subtle
destructive ring and footer styling.

<ComponentPreview name="base/card-destructive" />

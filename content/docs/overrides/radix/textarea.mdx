---
title: Textarea
---

## Extended Features

- Same as the original shadcn textarea component

## Install

Configure your registry in `components.json`:

```json
{
  "registries": {
    "@uxio": "https://ui.uxio.dev/r/styles/{style}/{name}.json"
  }
}
```

Then run:

```bash
npx shadcn@latest add @uxio/textarea
```

The correct variant (base or radix) is resolved automatically from your `components.json` style.

## Examples

### Default

Basic textarea with placeholder.

<ComponentPreview name="radix/textarea-default" />
